[project]
name = "make-brand"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kagglehub[pandas-datasets]>=1.0.0",
    "matplotlib>=3.10.8",
    "torch>=2.11.0",
]
